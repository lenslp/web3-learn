## 数据流重构
### 背景
交易页面，数据更新频率更，之前采用redux进行状态管理带来的性能瓶颈问题，每次dispatch action都会触发订阅组件的diff、不必要的重渲染、导致页面卡顿
### 解决
1. 引入mobx，重构数据流，响应式更新
    + 拆分（如行情、深度、交易记录）独立的 store，不同模块独立响应，避免无关组件重渲染。
    + 使用runInAction批处理数据更新，UI 只会在一帧内更新一次（MobX 自动优化）。
2. 逐步重构，先针对性能要求比较高的交易页面。