# AI Agent 技术架构与工作流程

> 创建日期：2026-02-21  
> 分类：AI / Agent / 技术架构

---

## 一句话理解

**AI Agent = 能自主"规划 + 决策 + 调用工具 + 执行"的 AI 工作者**，不像ChatGPT只能回答问题，Agent能帮你完成具体任务。

---

## Agent 核心架构（白板图）

```
┌─────────────────────────────────────────────────────────────┐
│                    AI Agent 核心架构                           │
└─────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │     用户输入     │
                    └────────┬────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │         Planning 阶段         │ ← 规划
              │   (思考：怎么做？)          │
              └─────────────┬────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │   RAG   │        │  Prompt │        │   CoT   │
   │  知识   │        │  工程   │        │ 思维链  │
   │  检索  │        │        │        │        │
   └─────────┘        └─────────┘        └─────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                            ▼
              ┌──────────────────────────────┐
              │        Execution 阶段         │ ← 执行
              │   (行动：怎么做？)          │
              └─────────────┬────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │  Code   │        │  Tool   │        │   MCP   │
   │Interpre-│        │   Use   │        │ (Model  │
   │  ter   │        │        │        │ Context  │
   │  代码   │        │  工具   │        │ Protocol)│
   │ 执行器  │        │  调用   │        │ 协议   │
   └─────────┘        └─────────┘        └─────────┘
```

---

## Planning 阶段（规划）

### 1. RAG - 知识检索增强

```
┌─────────────────────────────────────────┐
│              RAG 工作流程                 │
└─────────────────────────────────────────┘

用户问题: "帮我看看这个合同的潜在风险"
       │
       ▼
┌──────────────────┐
│   向量化 (Embedding) │ ← 把文本转为向量
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   向量数据库       │ ← Milvus / Pinecone / Chroma
│   相似度搜索      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   Top-K 文档      │ ← 找到最相关的K个文档
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  构造 Prompt      │ ← 把相关文档拼接进提示词
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   LLM 生成回答    │
└──────────────────┘
```

**解决的问题**：模型"不知道你私有数据" + 模型"容易胡说八道"

### 2. Prompt 工程

```
┌─────────────────────────────────────────┐
│           Prompt 常见技巧                 │
└─────────────────────────────────────────┘

1. Few-shot (少样本提示)
   ┌────────────────────────────┐
   │ 例：                      │
   │ 输入：2+2=？             │
   │ 输出：4                   │
   │                           │
   │ 输入：3+3=？             │
   │ 输出：？                  │
   └────────────────────────────┘

2. Chain-of-Thought (思维链)
   ┌────────────────────────────┐
   │ 例：                      │
   │ 问题：小明有5个苹果，     │
   │      吃了2个，又买了3个， │
   │      现在有几个？         │
   │                           │
   │ 思考：5-2+3=6            │
   │ 答案：6个                 │
   └────────────────────────────┘

3. Role-playing (角色扮演)
   ┌────────────────────────────┐
   │ 例：                      │
   │ "你是一位资深律师，       │
   │   请帮我审查这份合同..."  │
   └────────────────────────────┘

4. Tree-of-Thought (思维树)
   ┌────────────────────────────┐
   │        问题               │
   │       /  │  \            │
   │    方案A 方案B 方案C      │
   │     /|\   /|\   /|\       │
   │   ...  ...  ...          │
   │                           │
   │   评估所有分支，选择最优  │
   └────────────────────────────┘
```

### 3. CoT / ToT 思维链

```
┌─────────────────────────────────────────┐
│         CoT vs ToT 对比                 │
└─────────────────────────────────────────┘

CoT (Chain-of-Thought 思维链):
┌────────────┐    ┌────────────┐    ┌────────────┐
│   问题    │ → │ 思考过程  │ → │   答案    │
└────────────┘    └────────────┘    └────────────┘
                   (线性步骤)          

ToT (Tree-of-Thought 思维树):
        ┌────────────┐
        │   问题    │
       / \         / \
   ┌──────┐   ┌──────┐   ┌──────┐
   │方案A │   │方案B │   │方案C │
   │      │   │      │   │      │
   └──────┘   └──────┘   └──────┘
       \        |        /
        \   ┌───┴───┐   /
         \ /         \ /
        ┌──────────────┐
        │   评估选择   │
        │   最优方案   │
        └──────────────┘
        (树状搜索)
```

---

## Execution 阶段（执行）

### 1. Code Interpreter - 代码执行器

```
┌─────────────────────────────────────────┐
│         Code Interpreter 工作流程          │
└─────────────────────────────────────────┘

1. LLM 生成代码
   ┌────────────────────────────┐
   │  "计算 1-100 的素数和"   │
   └────────────┬─────────────┘
                │
                ▼
2. 发送到沙箱环境执行
   ┌────────────────────────────┐
   │  Docker 容器 / Python 进程 │
   └────────────┬─────────────┘
                │
                ▼
3. 执行并返回结果
   ┌────────────────────────────┐
   │  1060                     │
   └────────────┬─────────────┘
                │
                ▼
4. LLM 解读结果
   ┌────────────────────────────┐
   │  "1-100的素数和是1060"    │
   └────────────────────────────┘

典型应用：
- GPT-4 Code Interpreter
- Claude Code Runner
- 数学计算
- 数据分析
- 文件处理
```

### 2. Tool Use - 工具调用

```
┌─────────────────────────────────────────┐
│           Tool Use 工作流程              │
└─────────────────────────────────────────┘

Step 1: 定义工具
┌────────────────────────────────────┐
│  tools: [                           │
│    {                                │
│      "name": "weather",            │
│      "description": "查询天气",    │
│      "parameters": {               │
│        "type": "object",          │
│        "properties": {            │
│          "city": {"type": "string"}│
│        }                           │
│      }                             │
│    }                               │
│  ]                                  │
└────────────────────────────────────┘
                │
                ▼
Step 2: LLM 判断调用哪个工具
   "北京天气怎么样？" → 需要调用 weather(city="北京")
                │
                ▼
Step 3: 执行工具返回结果
   { "city": "北京", "temp": "15℃", "weather": "晴" }
                │
                ▼
Step 4: LLM 生成最终回答
   "北京今天天气晴，气温15℃。"

常见工具：
- 搜索 (Web Search)
- 文件读写 (File I/O)
- API 调用 (HTTP Request)
- 数据库查询 (Database)
- 浏览器控制 (Browser)
```

### 3. MCP - Model Context Protocol

```
┌─────────────────────────────────────────┐
│        MCP (Model Context Protocol)      │
│        = AI 世界的 USB 接口              │
└─────────────────────────────────────────┘

传统方式 (每个AI需要单独适配):
   ┌─────────┐     ┌─────────┐     ┌─────────┐
   │  ChatGPT │ ──→ │ Notion  │     │  Slack  │
   │   LLM    │     │   API   │     │   API   │
   └─────────┘     └─────────┘     └─────────┘
   每个AI都要写一个 Notion 适配器
   每个AI都要写一个 Slack 适配器
          ↓
MCP 方式 (统一协议):
   ┌─────────┐     ┌─────────────────┐
   │ 任意AI  │ ──→ │     MCP Server  │ ←→ Notion
   │  LLM    │     │   (统一接口)   │ ←→ Slack
   └─────────┘     │                 │ ←→ FileSystem
                   └─────────────────┘
                   (即插即用)

┌─────────────────────────────────────────┐
│           MCP 架构                       │
└─────────────────────────────────────────┘

┌─────────┐     ┌────────────┐     ┌─────────────────┐
│  Host   │ ←→ │   Client   │ ←→ │     Server      │
│ (LLM)   │    │  (MCP SDK) │    │ (各种服务)      │
└─────────┘     └────────────┘     └─────────────────┘
                            │
                            ▼
              ┌─────────────────────────┐
              │    Resources 资源        │ ← 文件、数据库
              │    Tools 工具           │ ← API、函数
              │    Prompts 提示词        │ ← 模板
              └─────────────────────────┘

MCP 生态:
- 文件系统 (File System)
- GitHub / Git
- Notion
- Slack
- PostgreSQL
- Puppeteer (浏览器)
- 等等...
```

---

## Multi-Agent 协作模式（多智能体）

### 1. Supervisor 模式（监督者）

```
┌─────────────────────────────────────────┐
│        Supervisor (监督者模式)           │
└─────────────────────────────────────────┘

                    ┌─────────────┐
                    │  Supervisor │
                    │   (管理者)   │
                    └──────┬──────┘
                           │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌─────────┐   ┌─────────┐   ┌─────────┐
    │ Agent A  │   │ Agent B  │   │ Agent C │
    │ (搜索)   │   │ (分析)   │   │ (整理)  │
    └─────────┘   └─────────┘   └─────────┘
         │               │               │
         └───────────────┼───────────────┘
                         │
                         ▼
                    ┌─────────────┐
                    │  Supervisor │
                    │  汇总结果   │
                    └─────────────┘

适用场景：
- 需要根据任务类型分配给不同Agent
- 需要一个"管理者"协调
```

### 2. Sequential 模式（顺序执行）

```
┌─────────────────────────────────────────┐
│       Sequential (顺序执行模式)          │
└─────────────────────────────────────────┘

┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ Agent 1 │ →  │ Agent 2 │ →  │ Agent 3 │ →  │  输出   │
│ 数据收集 │    │ 数据处理│    │ 结果整理 │    │        │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
   输出1          输出2          输出3
                            │
                            └────────────→ 最终结果

适用场景：
- 流水线式任务
- 每一步依赖上一步输出
- 数据处理流程
```

### 3. Network 模式（网状协作）

```
┌─────────────────────────────────────────┐
│         Network (网状协作模式)           │
└─────────────────────────────────────────┘

    ┌─────────┐
    │ Agent A │
    └────┬────┘
         │
    ┌────┼────┐
    │         │
    ▼         ▼
┌───────┐ ┌───────┐
│Agent B│ │Agent C│
└───┬───┘ └───┬───┘
    │         │
    └────┬────┘
         │
         ▼
    ┌─────────┐
    │ Agent D │ ← 结果汇总
    └─────────┘

特点：
- 去中心化
- 任意Agent可以相互通信
- 适合复杂协作场景
```

### 4. Hub-and-Spoke 模式（中心辐射）

```
┌─────────────────────────────────────────┐
│    Hub-and-Spoke (中心辐射模式)          │
└─────────────────────────────────────────┘

                ┌─────────────┐
                │     Hub     │
                │  (中央)    │
                └──────┬──────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
       ▼               ▼               ▼
   ┌─────────┐   ┌─────────┐   ┌─────────┐
   │  Spoke  │   │  Spoke  │   │  Spoke  │
   │   A     │   │   B     │   │   C     │
   └─────────┘   └─────────┘   └─────────┘

适用场景：
- 中央Agent作为协调中心
- 辐射出多个专业Agent
- 需要统一入口
```

---

## Agent 应用场景

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent 典型应用场景                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│      1. Research (研究)         │  │    2. Data Analysis (数据分析)  │
│                                 │  │                                 │
│  - 自动搜集资料                 │  │  - 读取数据文件                  │
│  - 总结论文                     │  │  - 统计分析                      │
│  - 写报告                       │  │  - 生成可视化                    │
│  - 整理引用                     │  │  - 解读结果                      │
└─────────────────────────────────┘  └─────────────────────────────────┘

┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│       3. Coding (编程)           │  │    4. Customer Service (客服)   │
│                                 │  │                                 │
│  - 写代码                       │  │  - 回答常见问题                  │
│  - 调试bug                      │  │  - 情绪安抚                      │
│  - 重构代码                     │  │  - 转接人工                      │
│  - 写测试                       │  │  - 记录工单                      │
└─────────────────────────────────┘  └─────────────────────────────────┘

┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│       5. Automation (自动化)      │  │    6. Education (教育)         │
│                                 │  │                                 │
│  - 定时执行任务                 │  │  - 个性化辅导                  │
│  - 监控系统状态                 │  │  - 解答问题                      │
│  - 自动回复                     │  │  - 制定学习计划                  │
│  - 批量处理                     │  │  - 批改作业                      │
└─────────────────────────────────┘  └─────────────────────────────────┘
```

---

## Agent vs Chatbot 对比

| 特性 | Chatbot | Agent |
|------|---------|-------|
| **能力** | 回答问题 | 完成任务 |
| **交互** | 被动响应 | 主动执行 |
| **工具** | 无 | 有 (Tool Use) |
| **规划** | 无 | 有 (Planning) |
| **记忆** | 短期 | 长期 + 向量存储 |
| **自主性** | 低 | 高 |
| **例子** | ChatGPT | AutoGPT, Claude Agent |

---

## Agentic Workflow（智能体工作流）

> Agentic Workflow = Agent + Workflow，让AI像人一样"思考→规划→执行→检查→改进"

```
┌─────────────────────────────────────────────────────────────┐
│              Agentic Workflow 工作流                          │
└─────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │   用户输入      │
                    └────────┬────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │    Reasoning (推理)            │ ← 思考过程
              │  - Chain-of-Thought 思维链    │
              │  - Reflection 反思/自检       │
              └─────────────┬────────────────┘
                            │
                            ▼
              ┌──────────────────────────────┐
              │    Planning (规划)            │ ← 制定计划
              │  - Thought 思考              │
              │  - Sub-task Decomposition    │
              │    (子任务分解)              │
              │  - Goal Refinement          │
              │    (目标优化)                │
              └─────────────┬────────────────┘
                            │
                            ▼
              ┌──────────────────────────────┐
              │    Tool Use (工具使用)        │ ← 执行行动
              │  - Tool Selection 选择工具   │
              │  - Tool Calling 调用工具    │
              │  - Output Parsing 输出解析  │
              └─────────────┬────────────────┘
                            │
                            ▼
              ┌──────────────────────────────┐
              │    Collaboration (协作)        │ ← 协作反馈
              │  - Memory 记忆              │
              │  - Knowledge 知识          │
              │  - Feedback 反馈            │
              └─────────────┬────────────────┘
                            │
                            ▼
                    ┌─────────────────┐
                    │   输出结果      │
                    └─────────────────┘
```

### 1. Reasoning (推理)

```
┌─────────────────────────────────────────────┐
│          Reasoning (推理)                    │
│     让AI像人一样"思考"                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Chain-of-Thought (思维链)                   │
├─────────────────────────────────────────────┤
│                                             │
│  原理：让AI展示思考过程，而非直接给答案    │
│                                             │
│  示例：                                     │
│  ┌────────────────────────────────────┐   │
│  │  问题：                             │   │
│  │  "小明有5个苹果，                  │   │
│  │   吃了2个，又买了3个，             │   │
│  │   现在有几个？"                     │   │
│  │                                     │   │
│  │  思考过程：                         │   │
│  │  1. 小明原来有5个苹果             │   │
│  │  2. 吃了2个，剩下 5-2=3个         │   │
│  │  3. 又买了3个， 3+3=6个           │   │
│  │                                     │   │
│  │  答案：6个                         │   │
│  └────────────────────────────────────┘   │
│                                             │
│  作用：                                     │
│  - 让推理过程可解释                         │
│  - 提高复杂问题回答准确率                   │
│  - 便于调试和纠正                          │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Reflection (反思/自检)                      │
├─────────────────────────────────────────────┤
│                                             │
│  原理：让AI在回答后检查自己是否正确        │
│                                             │
│  示例：                                     │
│  ┌────────────────────────────────────┐   │
│  │  第一次回答：                        │   │
│  │  "小明有6个苹果"                   │   │
│  │                                     │   │
│  │  反思：                            │   │
│  │  "等等，让我重新算一下：            │   │
│  │  5-2=3, 3+3=6                    │   │
│  │  嗯... 6是正确的                  │   │
│  │                                     │   │
│  │  最终回答：                         │   │
│  │  "小明有6个苹果" ✓                │   │
│  └────────────────────────────────────┘   │
│                                             │
│  作用：                                     │
│  - 减少错误回答                            │
│  - 自我纠错能力                            │
│  - 提高可靠性                              │
└─────────────────────────────────────────────┘
```

### 2. Planning (规划)

```
┌─────────────────────────────────────────────┐
│          Planning (规划)                     │
│       让AI像人一样"制定计划"                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Sub-task Decomposition (子任务分解)          │
├─────────────────────────────────────────────┤
│                                             │
│  原理：把大任务拆成小任务                   │
│                                             │
│  示例：                                     │
│  ┌────────────────────────────────────┐   │
│  │  大任务：                            │   │
│  │  "帮我分析这个产品的市场策略"         │   │
│  │                                     │   │
│  │  分解为子任务：                      │   │
│  │  1. 收集产品信息                    │   │
│  │  2. 分析目标用户                    │   │
│  │  3. 研究竞争对手                   │   │
│  │  4. 总结市场趋势                    │   │
│  │  5. 提出建议                        │   │
│  └────────────────────────────────────┘   │
│                                             │
│  作用：                                     │
│  - 处理复杂任务                            │
│  - 便于跟踪进度                           │
│  - 可以并行/串行执行                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Goal Refinement (目标优化)                   │
├─────────────────────────────────────────────┤
│                                             │
│  原理：根据情况动态调整目标                  │
│                                             │
│  示例：                                     │
│  ┌────────────────────────────────────┐   │
│  │  原始目标：                          │   │
│  │  "写一个App"                         │   │
│  │                                     │   │
│  │  分析约束条件：                      │   │
│  │  - 时间：只有2小时                  │   │
│  │  - 技能：只会前端                    │   │
│  │  - 工具：只有VS Code                │   │
│  │                                     │   │
│  │  优化后的目标：                      │   │
│  │  "用HTML/CSS/JS写一个单页Web App"   │   │
│  └────────────────────────────────────┘   │
│                                             │
│  作用：                                     │
│  - 适应现实约束                            │
│  - 制定可行计划                           │
│  - 避免目标过高/过低                      │
└─────────────────────────────────────────────┘
```

### 3. Tool Use (工具使用)

```
┌─────────────────────────────────────────────┐
│          Tool Use (工具使用)                │
│       让AI像人一样"使用工具"                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Tool Selection (选择工具)                    │
├─────────────────────────────────────────────┤
│                                             │
│  原理：根据任务选择合适的工具                │
│                                             │
│  决策过程：                                │
│  ┌────────────────────────────────────┐   │
│  │  任务：帮我查北京天气                │   │
│  │                                     │   │
│  │  分析：                              │   │
│  │  - 需要获取外部信息 → 需要工具       │   │
│  │  - 天气查询 → weather工具           │   │
│  │                                     │   │
│  │  选择：weather(city="北京")        │   │
│  └────────────────────────────────────┘   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Tool Calling (调用工具)                    │
├─────────────────────────────────────────────┤
│                                             │
│  原理：执行工具并获取结果                   │
│                                             │
│  执行流程：                                │
│  ┌────────────────────────────────────┐   │
│  │  1. 调用天气API                    │   │
│  │     weather(city="北京")            │   │
│  │                                     │   │
│  │  2. 获取结果                        │   │
│  │     {                                │   │
│  │       "city": "北京",               │   │
│  │       "temp": 15,                  │   │
│  │       "weather": "晴"              │   │
│  │     }                                │   │
│  │                                     │   │
│  │  3. 返回给LLM解读                   │   │
│  └────────────────────────────────────┘   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Output Parsing (输出解析)                  │
├─────────────────────────────────────────────┤
│                                             │
│  原理：解析工具返回的结果                    │
│                                             │
│  示例：                                    │
│  ┌────────────────────────────────────┐   │
│  │  原始输出：                          │   │
│  │  {                                   │   │
│  │    "city": "北京",                  │   │
│  │    "temp": 15                      │   │
│  │  }                                   │   │
│  │                                     │   │
│  │  解析后：                            │   │
│  │  "北京今天天气晴，气温15℃"          │   │
│  └────────────────────────────────────┘   │
│                                             │
│  作用：                                     │
│  - 提取关键信息                            │
│  - 格式化为人类可读                       │
│  - 准备下一轮输入                         │
└─────────────────────────────────────────────┘
```

### 4. Collaboration (协作)

```
┌─────────────────────────────────────────────┐
│          Collaboration (协作)                 │
│       让AI像人一样"协作学习"                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Memory (记忆)                              │
├─────────────────────────────────────────────┤
│                                             │
│  短期记忆（工作记忆）：                     │
│  - 当前对话上下文                          │
│  - 正在处理的任务                          │
│                                             │
│  长期记忆（知识库）：                       │
│  - 用户偏好设置                            │
│  - 历史交互记录                            │
│  - 学到的知识                             │
│                                             │
│  实现方式：                                │
│  - 向量数据库 (Pinecone, Milvus)          │
│  - 缓存 (Redis)                          │
│  - 文件系统                                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Knowledge (知识)                           │
├─────────────────────────────────────────────┤
│                                             │
│  来源：                                     │
│  - 外部文档 (PDF, Notion, Confluence)      │
│  - 数据库 (SQL, NoSQL)                     │
│  - API (第三方服务)                       │
│  - 网页内容                                │
│                                             │
│  使用方式（RAG）：                          │
│  1. 用户提问                              │
│  2. 检索相关知识                          │
│  3. 注入到Prompt                          │
│  4. LLM生成回答                           │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Feedback (反馈)                            │
├─────────────────────────────────────────────┤
│                                             │
│  来源：                                     │
│  - 用户直接反馈（纠正）                    │
│  - 执行结果反馈（成功/失败）               │
│  - 环境反馈（条件变化）                    │
│  - 自我反思                                │
│                                             │
│  处理方式：                                │
│  - 即时采纳（用户纠正）                    │
│  - 延迟学习（定期总结）                   │
│  - 反馈到记忆系统                         │
└─────────────────────────────────────────────┘
```

---

## Workflow 对比：传统 vs Agentic

```
┌─────────────────────────────────────────────────────────────┐
│              传统 Workflow vs Agentic Workflow                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────┐    ┌─────────────────────┐
│   传统 Workflow     │    │  Agentic Workflow   │
├─────────────────────┤    ├─────────────────────┤
│                     │    │                     │
│  固定流程           │    │  动态规划           │
│  Input → Step1      │    │  Input → 分析      │
│       → Step2      │    │        → 规划       │
│       → Step3      │    │        → 执行       │
│       → Output     │    │        → 检查       │
│                     │    │        → 输出       │
│                     │    │                     │
│  确定性执行         │    │  智能决策           │
│  每步预先定义       │    │  LLM决定下一步     │
│                     │    │                     │
│  难以处理例外       │    │  自动处理异常       │
│  失败即停止         │    │  尝试其他方法       │
│                     │    │                     │
│  低灵活性           │    │  高灵活性           │
│  适合简单重复任务   │    │  适合复杂开放任务  │
└─────────────────────┘    └─────────────────────┘
```

---

## 更新记录

| 日期 | 内容 |
|------|------|
| 2026-02-21 | 补充：Agentic Workflow 智能体工作流 |

---

## 技术栈推荐

```
┌─────────────────────────────────────────┐
│           Agent 开发技术栈               │
└─────────────────────────────────────────┘

框架:
├── LangChain (Python)
├── LangGraph (LangChain + 图)
├── LlamaIndex (Python)
├── AutoGen (微软)
├── CrewAI
└── OpenAI Agents SDK

向量数据库:
├── Pinecone
├── Weaviate
├── Milvus
├── Chroma
└── FAISS

工具:
├── SerpAPI (搜索)
├── Brave Search
├── Playwright (浏览器)
└── MCP Servers

部署:
├── Vercel AI SDK
├── Railway
├── Render
└── AWS Lambda
```

---

## 面试常见问题

### Q1: Agent 和传统程序有什么区别？

```
┌─────────────────────────────────────────┐
│  传统程序 vs Agent                      │
├─────────────────────────────────────────┤
│                                         │
│  传统程序:                               │
│  输入 → 固定逻辑 → 输出                  │
│  (确定性执行)                            │
│                                         │
│  Agent:                                 │
│  输入 → LLM推理 → 规划 → 工具调用 → 输出 │
│  (动态决策)                              │
│                                         │
│  核心区别: "确定性" vs "智能性"          │
└─────────────────────────────────────────┘
```

### Q2: 如何保证 Agent 的可靠性？

```
┌─────────────────────────────────────────┐
│        Agent 可靠性保证措施              │
└─────────────────────────────────────────┘

1. 错误处理
   - 工具调用失败重试
   - 降级策略
   - 异常状态捕获

2. 验证机制
   - 输出格式验证
   - 结果检查点
   - 人工审核入口

3. 监控日志
   - 完整执行轨迹
   - 关键节点记录
   - 性能监控

4. 安全防护
   - 敏感操作确认
   - 权限控制
   - 审计日志
```

---

## 更新记录

| 日期 | 内容 |
|------|------|
| 2026-02-21 | 初始版本：AI Agent 技术架构与工作流程 |

---

*持续更新中...*
