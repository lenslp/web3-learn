## BFC
+ 块级格式化上下文，创建一个独立的容器，在其中，元素的布局不会影响到外部元素，也不会受到外部元素的影响，通过使用 BFC，开发者可以实现一些布局效果，比如清除浮动、避免布局重叠。
+ 触发方式：
    - float：left、right 或 none
    - position：absolute 或 fixed
    - display：inline-block、flex、grid 或 table
    - overflow：hidden、scroll 或 auto
