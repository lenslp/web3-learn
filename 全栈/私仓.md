## 私有npm搭建
1. verdaccio
2. 已搭建：http://121.196.229.189:7001/
3. npm命令
    + 注册+登录：`npm adduser`
    + 登录：`npm login`
    + 退出登录：`npm logout`
    + 发布：`npm publish`
    + 查看用户名：`npm whoami`
    + 设置默认registry：`npm config set registry http://121.196.229.189:7001/`
4. 仓库地址：https://github.com/lenslp/c-npm

## verdaccio 和 npm官方私有包的区别
1. verdaccio：公司内网专用、数据完全掌控、需要自己负责运维、可以代理/缓存公网 npm。
2. npm官方私有包：在公网，外部协作更方便、省心但数据在第三方平台、免运维

## monorepo 管理工具
1. lerna
    + 解决 “多个包怎么发版/发布”（version/publish + 多包管理）
    + 结合workspaces，统一管理依赖
2. turborepo
    + 解决 “monorepo 跑任务太慢”（构建编排 + 增量构建缓存，不负责打包）
    + 处理多包之间的任务依赖关系，先 build 上游，再 build 下游
    + 只跑受影响的包
    + 构建缓存（本地缓存 + 远程缓存）
    + 结合workspaces，统一管理依赖
    + turbo run build：
        - 执行所有子包中的 build 脚本（Turbo 会确保 先构建依赖项，再构建依赖它的包）
        - 检查每个包的输入（源码文件、环境变量等）是否发生变化，只有当输入发生变化时，才会重新构建该包
        - 利用你的 CPU 多核优势， 并行 运行那些互不依赖的任务，从而加快整体构建速度
3. rush
    + 解决 “超大 monorepo 的工程治理”（依赖策略 + 变更管理 + 构建流程标准化）
4. workspaces（npm/yarn/pnpm）
    + 解决 在一个仓库里，如何“安装、链接、管理多个包的依赖关系
    + 统一依赖安装，所有 packages/apps 共享一个 lockfile，确保依赖版本一致、CI / 本地环境结果一致
    + 避免重复安装依赖，节省时间和磁盘空间
    + 自动创建软链接，方便在不同包之间共享代码
5. changesets
    + 自动化版本管理
    + 生成 Changelog

## 最佳实践
1. pnpm workspaces + turborepo + changesets
2. 仓库地址：https://github.com/lenslp/my-monorepo

## cli搭建
1. commander
    + 解析命令行参数
2. execa/cross-spawn
    + 执行 shell 命令
3. fs-extra
    + 操作文件系统（读写、删除、创建目录等）
4. download-git-repo
    + 从 git 仓库下载代码
5. prompts/inquirer
    + 交互式命令行提示
    + prompts：更轻量、依赖少，启动更快
    + inquirer：功能更全，但相对更重一些
6. chalk/ora
    + 终端输出美化
7. 自动更新
    + 自动更新 npm 包
8. 其他：
    + which: 判断某个命令是否已安装
9. bin
    + 命令行工具的入口文件

## ui组件库
1. storybook